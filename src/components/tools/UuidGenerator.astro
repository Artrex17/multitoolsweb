---
import type { Lang } from '../../i18n/strings';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;

const t = {
  es: { heading: 'Generador de UUID', generate: 'Generar UUID' },
  en: { heading: 'UUID generator', generate: 'Generate UUID' },
}[lang];
---

<section class="panel tool">
  <h1>{t.heading}</h1>
  <div class="actions">
    <button class="btn btn-primary" id="generate" type="button">{t.generate}</button>
  </div>
  <p class="screen" id="result"></p>
</section>

<script>
  document.querySelector('#generate')!.addEventListener('click', () => {
    document.querySelector('#result')!.textContent = crypto.randomUUID();
  });
</script>
